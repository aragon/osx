[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["lib"]
solc = "0.8.22"
optimizer = true
optimizer_runs = 200
extra_output = ["storageLayout"]

fs_permissions = [{ access = "write", path = "./artifacts" }]

[profile.ci]
fuzz = { runs = 512 }
