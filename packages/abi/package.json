{
  "name": "abi",
  "module": "index.ts",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build:contracts": "cd ../contracts && yarn build",
    "generate:abi": "yarn build:contracts && ./scripts/generate-abi.sh",
    "clean": "rimraf generated && rimraf index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@wagmi/cli": "^2.1.4",
    "dotenv": "^16.4.5",
    "rimraf": "^5.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}
