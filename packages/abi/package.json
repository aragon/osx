{
  "name": "@aragon/osx-abi",
  "module": "index.ts",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "build:contracts": "cd ../contracts && yarn build",
    "generate:abi": "./scripts/generate-abi.sh",
    "clean": "rimraf generated && rimraf index.ts",
    "publish:npm": "./scripts/publish.sh"
  },
  "devDependencies": {
    "@wagmi/cli": "^2.1.4",
    "rimraf": "^5.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}
